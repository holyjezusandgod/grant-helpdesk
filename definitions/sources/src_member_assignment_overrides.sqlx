config {
  type: "declaration",
  database: "bigtribebuilders",
  schema: "grant_helpdesk",
  name: "member_assignment_overrides",
  description: "App-written table: explicit member → grant coach overrides set from the UI. One row per member. Takes precedence over the first-assignment default in grant_member_assignments."
}
